import React from 'react'

const Btnpage = (props) => {
    return (
        <div>
            <button className='btn btn-primary py-2 px-4'>{props.name }</button>
        </div>
    )
}

export default Btnpage;